import React from "react"
import './LoginForm.css'

const LoginForm:React.FC=()=>{
    
    return(

        
        <>
        <div className="form">
            
        {/*<div className="background-image"></div>
        
       <form className="box-login">
            <form >
                <label className="form-label" htmlFor="email">Email address</label>
                <input className="form-input" type="email" placeholder="e.g. abcef@gmail.com" required autoFocus />
            </form>
            
            <form >
                <label className="form-label" htmlFor="password">Password</label>
                <input className="form-input"  type="password" required />
            </form>
            <button className="form-button" type="submit">Login</button>
    </form>
          <form className="box-signUp">
            <form >
                <label className="form-label" htmlFor="email">Email address</label>
                <input className="form-input"  type="email" placeholder="e.g. abcef@gmail.com" required autoFocus />
            </form>
            
            <form >
                <label className="form-label" htmlFor="password">Password</label>
                <input className="form-input"  type="password" required />
            </form>
            <form >
                <label className="form-label" htmlFor="password">Confirm password</label>
                <input className="form-input"  type="password" required />
            </form>
            <button className="form-button" type="submit">SignUp</button>
    </form>
      <form className="box-passwordChange">
            <form >
                <label className="form-label" htmlFor="password">Password</label>
                <input className="form-input"  type="password" required />
            </form>
            <form >
                <label className="form-label" htmlFor="password">Confirm new password</label>
                <input className="form-input"  type="password" required />
            </form>
            <button className="form-button"  type="submit">confirm</button>
</form>*/}
        </div>
        </>
    )
}
export default LoginForm